<%= form_with(model: degrees_user, local: true) do |form| %>
  <% if degrees_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(degrees_user.errors.count, "error") %> prohibited this degrees_user from being saved:</h2>

      <ul>
      <% degrees_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :degree_id %>
    <%= form.number_field :degree_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

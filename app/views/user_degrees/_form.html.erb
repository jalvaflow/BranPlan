<%= form_with(model: user_degree, local: true) do |form| %>
  <% if user_degree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_degree.errors.count, "error") %> prohibited this user_degree from being saved:</h2>

      <ul>
      <% user_degree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :degree_id %>
    <%= form.number_field :degree_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

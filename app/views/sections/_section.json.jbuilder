json.extract! section, :id, :section_id, :comment, :course, :section, :details, :status, :status_text, :enrolled, :limit, :waiting, :created_at, :updated_at
json.url section_url(section, format: :json)
